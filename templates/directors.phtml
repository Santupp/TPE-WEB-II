<?php require_once 'layout/header.phtml'; ?>

<h1>Lista de directores</h1>

<ul>
    <?php foreach ($directores as $director): ?>
        <li>
            <?= htmlspecialchars($director->nombre) ?>
            <a href="verPeliculasDirector/<?= $director->id ?>" class="a-animation">Ver películas</a>
            <img src="<?= htmlspecialchars($director->imagen) ?>" width="200" alt="Imagen del director">
            <?php if (isset($_SESSION['ID_USER'])): ?>
                <a href="eliminarDirector/<?= $director->id ?>" class="a-animation">Borrar</a>
            <?php endif; ?>
        </li>
    <?php endforeach; ?>

    <?php if (isset($_SESSION['ID_USER'])):
        require_once 'templates/form.add.director.phtml';
    endif; ?>
</ul>