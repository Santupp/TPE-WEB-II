<?php require('layout/header.phtml') ?>
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles de la Película</title>
</head>
<body>

<h1>Detalles de la Película</h1>

<?php if (isset($film)): ?>
    <table>
        <tr>
            <th>ID</th>
            <td><?= htmlspecialchars($film->id) ?></td>
        </tr>
        <tr>
            <th>Título</th>
            <td><?= htmlspecialchars($film->nombre) ?></td>
        </tr>
        <tr>
            <th>Género</th>
            <td><?= htmlspecialchars($film->genero) ?></td>
        </tr>
        <tr>
            <th>Director</th>
            <td><?= htmlspecialchars($film->director_name) ?></td>
        </tr>
        <tr>
            <th>Año</th>
            <td><?= htmlspecialchars($film->fecha_estreno) ?></td>
        </tr>
        <tr>
            <th>Imagen</th>
            <td> <img src="<?= htmlspecialchars($film->imagen) ?>" width="200"> </td>
        </tr>
    </table>
<?php else: ?>
    <p>No se encontraron detalles de la película.</p>
<?php endif; ?>

</body>
</html>