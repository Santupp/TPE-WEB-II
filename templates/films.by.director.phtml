<?php require('layout/header.phtml') ?>
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Películas de <?= htmlspecialchars($director) ?></title>
</head>
<body>
    <h1>Películas de <?= htmlspecialchars($director) ?></h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Título</th>
                <th>Género</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($peliculas as $pelicula): ?>
                <tr>
                    <td><?= htmlspecialchars($pelicula->id) ?></td>
                    <td><?= htmlspecialchars($pelicula->nombre) ?></td>
                    <td><?= htmlspecialchars($pelicula->genero) ?></td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>

</body>
</html>